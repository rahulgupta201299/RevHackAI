import { createTheme } from '@mui/material/styles';
export default createTheme({
  palette: {
    background: { default: '#faf4e8' },
    primary: { main: '#151515' },
    secondary: { main: '#ff4d08' },
  },
  typography: { fontFamily: 'Inter, Arial, sans-serif', button: { fontWeight: 800 } },
});
